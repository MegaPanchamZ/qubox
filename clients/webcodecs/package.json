{
  "name": "webcodecs-client",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@digitaldefiance/reed-solomon-erasure.wasm": "1.0.2",
    "rkyv": "0.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0",
    "@types/dom-webcodecs": "^0.1.9",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vite": "^6.0.0",
    "vitest": "^1.0.0"
  }
}